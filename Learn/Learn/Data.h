#if !defined(DATA)
#define DATA
#include <iostream>
#include <vector>
#include <string>

using namespace std;
class Data
{
public:
	void SetData(string str);

	vector<int> m_DataVec;

private:

};

#endif